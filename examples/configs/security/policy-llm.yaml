# Inference Authorization (experimental)
#
# Authorizes OpenAI-style inference calls on the model in the request body. The
# `policy` filter loads a policy that declares `llm:` routes, so it buffers the
# request, reads the top-level `model`, and evaluates that model against the
# policy — `require`/`deny` rules keyed on `llm.model_id`, the caller's identity,
# and the promoted sampling parameters (`custom.llm.*`).
#
# The model is parsed by the proxy from the body the backend will parse, so a
# client header cannot make policy authorize a different model than the one
# served. No protocol classifier is needed: inference calls carry no JSON-RPC
# envelope, and the filter attributes them itself.
#
# Three gates fail closed by default: a model no `llm:` route selects is denied
# (`llm.require_route`), a body with no usable `model` is denied
# (`llm.require_model`), and a body carrying both a JSON-RPC envelope and a
# top-level `model` is denied as ambiguous.
#
# This authorizes the APIs that name the model in the body. An API that names it
# in the URL instead — Azure OpenAI's
# `/openai/deployments/{deployment}/chat/completions`, Bedrock's
# `/model/{id}/invoke` — carries no top-level `model`, so every such request
# would be denied for carrying none. The router below is scoped to the
# body-addressed paths for that reason.
#
# A method that carries no body is not an inference call, so `GET /v1/models`
# and CORS preflights skip these gates. Identity still governs them.
#
# `config_path` points at the policy document (plugins + `llm:` routes)
# that the filter loads once at construction. It is operator-supplied,
# not shipped here: a real policy carries your own issuers, secrets, and
# per-model allow-lists. See `tests/integration/fixtures/llm-policy.yaml`
# for the minimal single-HS256-identity-plugin policy the integration
# test loads — point `config_path` at that file to run this example
# as-is.
#
# Requires building with the `policy-engine` feature, which is off
# by default:
#
# Usage:
#   cargo run -p praxis-proxy --features policy-engine -- -c examples/configs/security/policy-llm.yaml
#
# Exercise (assumes the backend at :3000 echoes 200, and `config_path`
# points at tests/integration/fixtures/llm-policy.yaml):
#
#   # 401: no credential — the identity gate runs before the body matters
#   curl -i -X POST http://localhost:8080/v1/chat/completions \
#     -H "Content-Type: application/json" \
#     -d '{"model":"gpt-4o-mini","messages":[]}'
#
#   # 403: no usable top-level `model` — fails closed (llm.require_model)
#   curl -i -X POST http://localhost:8080/v1/chat/completions \
#     -H "Content-Type: application/json" \
#     -d '{"messages":[]}'
#
# The remaining paths need an HS256 JWT signed with the fixture's
# `secret`, for `iss: https://idp.example.com` and
# `aud: praxis-policy-example`, carrying a `roles` claim. See
# `mint_fixture_jwt` in
# `tests/integration/tests/suite/examples/policy_llm.rs` for the exact
# claim set, then:
#
#   # 200: a model the caller's role is allowed to use
#   curl -H "Authorization: Bearer $JWT" -d '{"model":"gpt-4o-mini","messages":[]}' \
#     http://localhost:8080/v1/chat/completions
#
#   # 403: a model the policy reserves for the `research` role
#   curl -H "Authorization: Bearer $JWT" -d '{"model":"gpt-4o","messages":[]}' \
#     http://localhost:8080/v1/chat/completions

listeners:
  - name: default
    address: "127.0.0.1:8080"
    filter_chains:
      - main

filter_chains:
  - name: main
    filters:
      # Inference authorization. `config_path` points at the policy document,
      # whose `llm:` routes are what switch this path on. The optional `llm:`
      # block only tunes it.
      - filter: policy
        config_path: /etc/praxis/llm-policy.yaml
        llm:
          require_model: true # optional; default true — deny a body with no model
          provider: openai # optional; recorded on `llm.provider`

      # Scoped to the body-addressed inference APIs: this path reads the model
      # out of the body, so a URL-addressed provider must not be routed here.
      - filter: router
        routes:
          - path_prefix: "/v1/"
            cluster: inference

      - filter: load_balancer
        clusters:
          - name: inference
            endpoints:
              - "127.0.0.1:3000"

insecure_options:
  allow_private_endpoints: true # example proxies to local backends
